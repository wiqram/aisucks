# Non-secret build/push knobs. Consumed by docker-compose-prod.yml via
# `--env-file .env.production` in the Jenkinsfile. NO SECRETS here — this file is
# committed. Secrets (when the idea needs them) belong in Vault + SOPS, never here.
NEXT_PUBLIC_SITE_URL=https://aisucks.predictonomy.com
