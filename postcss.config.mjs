// Tailwind v4 is a PostCSS plugin — no tailwind.config.js needed. Theme/config lives
// in CSS via @theme in app/globals.css.
const config = {
  plugins: ['@tailwindcss/postcss']
};

export default config;
